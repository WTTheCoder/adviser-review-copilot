# Candidate Fact Extraction Fixtures

These fixtures are deterministic Phase 5 examples for future extraction evaluation. Normal CI does not call OpenAI.

Future live-model evaluation can run these notes through the `CandidateFactExtractor` contract, validate the structured output schema, and compare extracted fields plus human-review requirements against the expected values.
